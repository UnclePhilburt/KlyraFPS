[build]
  publish = "docs"

[build.environment]
  GIT_LFS_ENABLED = "true"

# FPS subdomain serves the game
[[redirects]]
  from = "/*"
  to = "/game.html"
  status = 200
  force = true
  [redirects.conditions]
    Host = ["fps.klyra.lol"]

# Root domain serves landing page (default index.html behavior)
